---
- name: Install docker
  shell: curl -fsSL get.docker.com | sh

- name: Install pip
  become: true
  apt:
    name:
      - python3-pip
    update_cache: yes

- name: Install Docker Python SDK
  become: true
  pip:
    name: docker
